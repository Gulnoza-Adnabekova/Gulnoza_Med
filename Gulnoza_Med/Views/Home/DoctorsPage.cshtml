@using Gulnoza_Med.Models
@model List<Doctors>

<section class="team_section layout_padding">
    <div class="container">
        <div class="heading_container heading_center">
            <h2>
                Our <span>Doctors</span>
            </h2>
        </div>
        <div style="display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 3fr));
        gap: 20px;
        padding: 20px;">

            @foreach (var item in Model)
            {
                <div class="item">
                    <div class="box">
                        <div class="img-box">
                            <img src="/@item.ImageUrl" alt="@item.Name" />
                        </div>
                        <div class="detail-box">
                            <a asp-controller="Home" asp-action="DoctorDetailsPage" asp-route-id="@item.Id">
                                @item.Name
                            </a>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</section>

<!-- end team section -->
<!-- info section -->
